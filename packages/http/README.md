# MCP HTTP Server

Lean MCP server for safe, configurable HTTP requests.

## What it provides
- `http.request` for full control
- `http.get` and `http.post` convenience tools

## Quick start
```
npx @waleedyousaf07/mcp-http@latest
```

For full usage and config, see `setup.md`.

